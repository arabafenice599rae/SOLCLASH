{
  "name": "solclash-events",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "TypeScript side of the SOLCLASH-EVENTS Anchor workspace: IDL consumers and integration tests.",
  "scripts": {
    "lint": "prettier --check \"tests/**/*.ts\"",
    "lint:fix": "prettier --write \"tests/**/*.ts\"",
    "typecheck": "tsc --noEmit",
    "test": "ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.32.1"
  },
  "devDependencies": {
    "@types/bn.js": "^5.1.6",
    "@types/chai": "^4.3.20",
    "@types/mocha": "^10.0.10",
    "chai": "^4.5.0",
    "mocha": "^10.8.2",
    "prettier": "^3.4.2",
    "ts-mocha": "^10.1.0",
    "typescript": "^5.7.3"
  }
}
